# ToonFlow

Go project.

## Skills

### codegraph

When you need to understand the codebase structure, trace relationships, or visualize dependencies:

1. Run `codegraph` to generate a dependency graph of the project
2. Use the output to identify key modules, entry points, and coupling points
3. Reference the graph when planning changes or explaining architecture

### Superpowers

Always check for applicable skills before responding. Use `Skill` tool to invoke them.
